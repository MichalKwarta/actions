name: Commands
on: 
  issue_comment:
      types: [created, edited, deleted]

jobs:
  pr_commented:
    permissions: write-all

    # This job only runs for pull request comments
    name: test
    if: github.event.issue.pull_request && contains(github.event.comment.body, '/test')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/github-script@v6
        env:
          body: "👋 Hey, test job was triggered!"
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: |
            const script = require('.github/scripts/dist/commentPR.js')
            await script({github, context, core})

         
      - uses: actions/github-script@v6
        id: get-pr
        with:
          script: |
            const script = require('.github/scripts/dist/getPR.js')
            const tmp = await script({github, context, core})
            console.log(tmp)
            return tmp
      
      - uses: actions/checkout@v3
        with:
          repository: ${{ fromJSON(steps.get-pr.outputs.result).head.repo.full_name }}
          ref: ${{ fromJSON(steps.get-pr.outputs.result).head.sha }} # or .head.ref for branch name

      # - uses: actions/setup-python@v4
      #   with:
      #     python-version: '3.10'
      
      # - name: Install pipenv
      #   run: pip install pipenv

      # - name: Run tests
      #   run: |
      #     pipenv install --dev
      #     pipenv run pytest
      - name: report test result if failed
        if: failure()
        uses: actions/github-script@v6
        env:
            body: "👋 Hey, test job failed! :("
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: |
            const script = require('.github/scripts/dist/commentPR.js')
            await script({github, context, core})
            
      - name: report test result if success
        if: success()
        uses: actions/github-script@v6
        env:
            body: "👋 Hey, test job successfully completed!"
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: |
            const script = require('.github/scripts/dist/commentPR.js')
            await script({github, context, core})
      
